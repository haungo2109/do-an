import React from 'react';
import { View, Text } from 'react-native';
// import styled from 'styled-components/native';

function UserScreen(props) {
	return (
		<>
			<View>
				<Text>Ahi user view</Text>
			</View>
		</>
	);
}

export default UserScreen;
